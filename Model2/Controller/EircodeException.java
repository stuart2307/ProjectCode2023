public class EircodeException extends Exception {
    public EircodeException(String msg){ //Creating an exception specifically for blank entries

        super(msg); //invoking the constructor of the superclass
    } 
}
